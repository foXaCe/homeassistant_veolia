# État de l'overhaul — ha-integration-overhaul

Branche : `refactor/integration-overhaul-2026-07-10` (depuis `refactor/unvendor-veolia-api` @ cf9bea2)
Tag backup : `backup/before-overhaul-20260710-152030`
Démarré : 2026-07-10

## Contexte

- Intégration déjà overhaulée une fois (62c0742) puis dé-vendorisée (PyPI `veolia-api-foxace==2.3.0`, PR #12 mergée).
- 10/11 plans `improve` exécutés sur la branche unvendor (PR #13 ouverte, intacte).
- **Décisions utilisateur (2026-07-10)** : branche dédiée ✓ ; plan 008 INCLUS (feu vert — déplacer tests client API vers repo veolia-api en Phase 8) ; inventaire Phase 1.6 VALIDÉ, périmètre complet (system_health + docs-examples + retry_after) ; **quality_scale → "platinum"** si checklist Gold+Platinum verte en Phase 7.
- Baseline qualité au départ : 144 tests verts, mypy strict 0 erreur, ruff propre (dixit plans/README.md — à re-vérifier Phase 1).

## Phases

- [x] Phase 0 — Préflight (tag backup, branche, HA dev Up, main ff 7a5a32b)
- [x] Phase 0.5 — État de l'art (doc : scratchpad/ha-backend-state-of-art-2026-07-10.md ; résumé ci-dessous)
- [x] Phase 1 — Découverte (inventaire ci-dessous ; baseline perf ✓ ; en attente validation utilisateur 1.7)
- [x] Phase 2 — Architecture (365fd07 — conforme état de l'art, 0 changement ;
      retry_after NON adopté : client 2.3.0 ne propage pas Retry-After + poll 6 h)
- [x] Phase 3 — Refactor structurel (0 changement : ruff F401/F841/F811 propre,
      py_compile OK, 0 cycle ; pycache orphelin veolia_api/ purgé repo + HA dev)
- [x] Phase 4 — Client externe (0 changement : timeout/retry/redaction password OK
      dans client épinglé ; redaction identifiants = commits upstream non publiés)
- [x] Phase 5 — Entités (0 changement : EntityDescription + value_fn partout,
      PARALLEL_UPDATES=0, actions → HomeAssistantError traduit ; device_class DATE
      évité volontairement pour last_date/next_payment — documenté dans sensor.py)
- [x] Phase 6 — Config flow & translations (0 changement : selectors modernes
      partout, autocomplete, vouvoiement strict, parité EN/FR vérifiée)
- [x] Phase 7 — Qualité transverse : AJOUTS — system_health.py (+ test ×2 +
      traductions ×3), docs/automations.md (+ lien README), manifest
      quality_scale silver → **platinum** (décision utilisateur) ; ruff ✓
      mypy strict ✓ (15 fichiers) ; hassfest local Docker ✓ (0 invalid) ;
      deploy + restart 15:50
- [x] Phase 8 — Tests : plan 008 exécuté (3 cas portés dans veolia-api → 92 verts
      là-bas, WORKING TREE NON COMMITÉ à revoir par l'utilisateur ;
      tests/veolia_api/ supprimé ici) + test DST CET→CEST ajouté
      (piège : soustraction same-tzinfo = wall-clock, comparer en UTC) ;
      150 tests verts, coverage 100 % (684 stmts, 15 modules)
- [ ] Phase 9 — Validation finale (supprimer ce fichier au commit final)

## Baseline perf (Phase 1.5.bis — mesurée 2026-07-10 15:29, HA dev, code branche déployé)

```
Setup of domain veolia took 0.00 seconds
Premier refresh coordinator (login + fetch API cloud) : ~5,2 s (réseau, hors setup)
0 erreur au boot
```

importtime local (.venv Python 3.14, cumulatif — les imports HA core sont pré-chargés en prod) :

```
homeassistant.helpers.aiohttp_client : 472 ms (chaîne HA core)
homeassistant.components.recorder    : 291 ms (via veolia.statistics)
homeassistant.components.zeroconf    : 290 ms (chaîne HA core)
custom_components.veolia (total)     : 994 ms cumulé local
```

## État de l'art HA backend (Phase 0.5 — 2026-07-10, HA dev 2026.7.1)

À appliquer / vérifier pendant les passes 2-7 :

1. **Coordinator (2025.11)** : `UpdateFailed(err, retry_after=N)` pour honorer un
   `Retry-After` HTTP 429 de l'API cloud — à adopter. Retriggering natif du refresh
   (plus besoin de guard manuel si présent).
2. **Config flow (2026.6)** : combiner un update-listener avec
   `async_update_reload_and_abort()` / `reload_on_update=True` = déprécié, **erreur dès
   2026.12** — vérifier config_flow.py (utilise déjà `OptionsFlowWithReload` a priori).
3. **Advanced mode (2026.5)** : `show_advanced_options` déprécié, retrait 2027.6 —
   remplacer par des `Section` si utilisé.
4. **Statistiques recorder** : `has_mean` retiré Core 2026.11 → `mean_type` ;
   `unit_class` obligatoire depuis 2025.11 — repo déjà conforme (statistics.py).
5. **OptionsFlow** : setter `self.config_entry` cassé depuis 2025.12 — repo déjà OK.
6. **Quality Scale du jour** : 20 règles Bronze, 10 Silver, 21 Gold, 3 Platinum.
   `manifest.json` déclare `quality_scale: silver` mais pas de `quality_scale.yaml` —
   à créer si on documente le niveau. Doute agent sur repairs.py (le commit cf9bea2
   a ajouté les repair issues — vérifier la matérialité en Phase 1).
7. Sans impact direct : subentries (2025.2+), unique_id collision warning (2025.3),
   `home_assistant_start` (2026.6/7), `UnitOfDensity`/`UnitOfRatio` (2026.7),
   `DeviceEntry.suggested_area` (2025.08).
8. Releases 2026.5/6/7 : aucun breaking direct pour une intégration cloud_polling.

## Inventaire unique_id (Phase 1.5 — extrait du core.entity_registry HA dev, account_id=1235075, 17 entités)

Format code : `f"{account_id}_{description.key}"` (entity.py:38). Config entry : version=2, unique_id="1235075".

```
1235075_last_index                      sensor.veolia_1235075_index_compteur
1235075_daily_consumption               sensor.veolia_1235075_conso_journaliere
1235075_monthly_consumption             sensor.veolia_1235075_conso_mensuelle
1235075_annual_consumption              sensor.veolia_1235075_conso_annuelle
1235075_last_date                       sensor.dernier_releve
1235075_balance                         sensor.veolia_1235075_solde_du_compte
1235075_monthly_payment                 sensor.veolia_1235075_mensualite
1235075_next_payment                    sensor.veolia_1235075_prochain_prelevement
1235075_billing_index                   sensor.veolia_1235075_index_de_facturation
1235075_daily_sms_alert_switch          switch.veolia_sms_alerte_journaliere
1235075_monthly_sms_alert_switch        switch.veolia_1235075_sms_alerte_mensuelle
1235075_unoccupied_alert_switch         switch.veolia_1235075_alerte_logement_vide
1235075_daily_threshold_text            text.veolia_1235075_seuil_alerte_journaliere_en_l
1235075_monthly_threshold_text          text.veolia_1235075_seuil_alerte_mensuelle_en_m3
1235075_daily_alert_binary_sensor       binary_sensor.veolia_1235075_alerte_conso_journaliere
1235075_monthly_alert_binary_sensor     binary_sensor.veolia_1235075_alerte_conso_mensuelle
1235075_unoccupied_alert_binary_sensor  binary_sensor.veolia_1235075_alerte_logement_vide
```

Statistic ids externes : `veolia:{account_id}_{série}` (3 séries, coordinator.py:114).
**Décision : format conservé tel quel — aucune migration nécessaire.**

## Synthèse découverte (Phase 1 — 2026-07-10)

État exceptionnel au départ : ruff strict propre, mypy --strict 0 erreur, 169 tests
collectés, coverage 100 % (.coverage), Bronze ✅ Silver ✅ Gold quasi-complet
(repairs ajoutées par cf9bea2 — matérialisées dans __init__.py + coordinator, doute
Phase 0.5 levé), Platinum : les 3 règles (async-dependency, inject-websession,
strict-typing) déjà remplies. Déployé HA dev = repo (diff vide).

Restes identifiés :
1. **veolia-api (frère)** : 16 commits non publiés au-delà de v2.3.0 dont un FIX
   SÉCURITÉ (redaction identifiants de compte dans logs debug) — pin manifest
   =2.3.0. La décision de release appartient à l'utilisateur.
2. **Plan 008 (feu vert)** : tests/veolia_api/ (22 tests) testent le package
   installé → porter 3 cas upstream (inventaire dans plans/008), supprimer les
   doublons ici.
3. **docs-examples (Gold)** : pas d'exemples d'automatisations dans la doc.
4. **quality_scale manifest** : déclaré "silver", code au-delà — bump à décider.
5. **system_health.py absent** (optionnel — can_reach_server pertinent en cloud).
6. Cruft local : custom_components/veolia/veolia_api/__pycache__ orphelin (repo +
   HA dev, non suivi) → rm. `.claude/worktrees/` 162 Mo dans veolia-api (utilisateur).
7. `.venv` shebangs cassés (repo déplacé) → utiliser `.venv/bin/python3 -m <outil>`.
8. État de l'art à appliquer : UpdateFailed(retry_after=) si l'API expose le 429 ;
   vérifier absence listener+reload combinés dans config_flow.
9. Dette mineure : accès attributs bruts via VeoliaModel.__getattr__ (raw: Any)
   dans sensor.py:148-158 et model.py (clés françaises payload) — assumé/documenté.

## TODO / notes

- Déployé = branche depuis 15:29 (rsync --delete).
- plans/ untracked : ne pas toucher, appartient à l'utilisateur.
- Fichiers > 200 l. : coordinator.py 245, model.py 335, config_flow.py 343 —
  cohésifs, pas de duplication → pas de split forcé (à confirmer en Phase 3).
